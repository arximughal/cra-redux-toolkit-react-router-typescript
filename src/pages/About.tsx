import React from "react";

// Define an about page component that renders some content
const About: React.FC = () => {
  // Return the JSX element for the about page component
  return (
    <div>
      <h1>About React Boilerplate</h1>
      <p>
        This is a boilerplate code created by Microsoft Bing search chat mode.
      </p>
      <p>
        It uses CRA, react router and redux toolkit to create a scalable and
        maintainable React app.
      </p>
    </div>
  );
};

export default About;
